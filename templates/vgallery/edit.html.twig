{% extends 'base_dashboard.html.twig' %}

{% block title %}Edit {{ vgallery.title }}{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h6><i class="bi bi-pencil"></i> Edit Gallery: {{ vgallery.title }}</h6>

                <a href="{{ path('app_vgallery_show', {'id': vgallery.id}) }}" class="btn btn-light-primary">
                    <i class="bi bi-eye"></i> View Gallery
                </a>


        </div>

        {{ include('vgallery/_form.html.twig') }}
    </div>
{% endblock %}
