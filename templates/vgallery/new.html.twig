{% extends 'base_dashboard.html.twig' %}

{% block title %}New Gallery{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h6><i class="bi bi-images"></i> Create New Gallery</h6>
            <a href="{{ path('app_vgallery_index') }}" class="btn btn-light-secondary">
                <i class="bi bi-arrow-left"></i> Back to Galleries
            </a>
        </div>

        {{ include('vgallery/_form.html.twig') }}
    </div>
{% endblock %}
