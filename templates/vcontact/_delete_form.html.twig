<form method="post" action="{{ path('app_vcontact_delete', {'id': vcontact.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vcontact.id) }}">
    <button class="btn">Delete</button>
</form>
