{% extends 'base_dashboard.html.twig' %}
{% block title %}Edit Team{% endblock %}
{% block body %}
    <div class="row mb-4">
        <div class="col">
            <div class="d-flex justify-content-between align-items-center">
                <h5 class="mb-0 text_color_default">Edit Team Members</h5>
                <nav class="breadcrumb-nav">
                    <a href="{{ path('app_dashboard') }}">Home</a>
                    <i class="bi bi-chevron-right mx-2"></i>
                    <a href="{{ path('app_vteam_index') }}">Staff</a>
                    <i class="bi bi-chevron-right mx-2"></i>
                    <span>Edit Team Members</span>
                </nav>
            </div>
        </div>
    </div>

    {{ include('vteam/_form.html.twig', {'button_label': 'Update'}) }}
    <div class="container-fluid mt-5 d-flex justify-content-end">
        {{ include('vteam/_delete_form.html.twig') }}
    </div>

{% endblock %}
